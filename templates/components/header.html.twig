<header>
	<nav class="navbar navbar-expand-lg sticky-top">
		<div class="container sticky-top">
			<a class="navbar-brand" href="{{path('app')}}">
				<img src="{{ asset('logo-no-background.svg') }}" alt="LOGO" height="50">
			</a>
			<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
				<span class="navbar-toggler-icon"></span>
			</button>
			<div class="collapse navbar-collapse" id="navbarNav">
				<ul class="navbar-nav ms-auto">
					{% if not app.user %}
						<li class="nav-item">
							<a href="{{path('app_register')}}" class="nav-link text-black-50">
								<i class="fas fa-user-plus"></i>
								INSCRIPTION</a>
						</li>
					{% endif %}
					<li class="nav-item">
						<a class="nav-link text-black-50" href="{{path('app_rides')}}">
							<i class="fas fa-eye"></i>
							VOIR TOUS LES TRAJETS</a>
					</li>
					<li class="nav-item">
						<a class="nav-link text-black-50" href="{{path('app_add_ride')}}">
							<i class="fas fa-plus"></i>
							PROPOSER UN TRAJET</a>
					</li>
					{% if not app.user %}
						<li class="nav-item">
							<a class="nav-link text-black-50" href="{{path('app_login')}}">
								<i class="fas fa-sign-in-alt"></i>
								SE CONNECTER</a>
						</li>
					{% else %}
						<li class="nav-item">
							<a class="nav-link text-black-50" href="{{path('app_dashboard')}}">
								<i class="fas fa-user"></i>
								MON PROFIL</a>
						</li>
						<li class="nav-item">
							<a class="nav-link text-black-50" href="{{path('app_logout')}}">
								<i class="fas fa-sign-out-alt"></i>
								SE DECONNECTER</a>
						</li>
					{% endif %}
				</ul>
			</div>
		</div>
	</nav>
</header>
